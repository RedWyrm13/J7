\documentclass[conference]{IEEEtran}

% ── Packages ──────────────────────────────────────────────────────────────
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{cite}
\usepackage{xcolor}
\usepackage[caption=false,font=footnotesize]{subfig}
\usepackage{url}
\usepackage[hidelinks]{hyperref}

% ── Macros ────────────────────────────────────────────────────────────────
\newcommand{\todo}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\nqubits}{n}
\newcommand{\shots}{s}

% ── Title Block ───────────────────────────────────────────────────────────
\title{Polynomial-Resource Classification of Quantum Circuit Families via Classical Shadows}

\author{
\IEEEauthorblockN{Andrew Maciejunes}
\IEEEauthorblockA{
Virginia Modeling, Analysis and Simulation Center\\
Old Dominion University\\
Norfolk, VA, USA\\
amacieju@odu.edu
}
\and
\IEEEauthorblockN{Ross Gore}
\IEEEauthorblockA{
Center for Secure and Intelligent Critical Systems\\
Old Dominion University\\
Norfolk, VA, USA\\
rgore@odu.edu
}
}

\begin{document}

\maketitle

\begin{abstract}
We demonstrate that three structurally distinct quantum circuit families---Instantaneous Quantum Polynomial (IQP), Clifford, and Clifford$+T$---can be reliably distinguished using only polynomial measurement resources.
The central methodological contribution is the application of classical shadows~\cite{Huang2020} to extract all six two-qubit Pauli correlators ($ZZ$, $XX$, $YY$, $XY$, $XZ$, $YZ$) across all qubit pairs, yielding $O(\nqubits^2)$ features from a shot budget that scales as $\shots = 16\nqubits^2$---entirely polynomial in system size.
We compare three measurement strategies: a $Z$-basis-only baseline (all-pairs $ZZ$ correlations), a multi-basis approach (global $Z$/$X$/$Y$ rotations giving $ZZ$, $XX$, and $YY$ correlators at $3\times$ the shot cost), and classical shadows (all six Pauli pair correlators at the same shot cost as the baseline).
Four classical classifiers are evaluated across 4--20 qubits with $N_c = 1000$ circuits per family.
A cross-domain experiment further demonstrates that classifiers trained on circuit-sampled data successfully classify aggregated spectrum observations drawn from the same distribution classes, providing evidence that the polynomial feature set captures distribution-level properties that generalize across data modalities.
\todo{Summarize key accuracy result and scaling trend.}
\end{abstract}

\begin{IEEEkeywords}
classical shadows, quantum circuit families, IQP circuits, Clifford circuits, polynomial resources, distribution classification, Pauli correlators, quantum machine learning
\end{IEEEkeywords}

\section{Introduction}
\label{sec:intro}

A fundamental question in quantum information is whether exponentially large probability distributions---the output distributions of quantum circuits---can be meaningfully characterized using only polynomially many measurements.
A naive approach based on estimating the full probability vector requires exponentially many samples.
However, low-order statistics (marginals, correlators) can be estimated with far fewer measurements and may nevertheless carry sufficient information to distinguish structurally distinct distribution classes.

Quantum circuit families provide a principled testbed for this question.
IQP, Clifford, and Clifford$+T$ circuits produce distributions with qualitatively different statistical signatures, rooted in their distinct computational complexity properties.
Clifford circuits are classically simulable; IQP sampling is believed to be classically hard under plausible complexity-theoretic conjectures~\cite{Bremner2016}; Clifford$+T$ circuits interpolate between the two.
If these families can be distinguished with polynomially many measurements, it suggests that their distributional signatures are encoded in low-order statistics that a polynomial measurement protocol can access.

The key methodological challenge is choosing the right measurement protocol.
Measuring in a single basis ($Z$-only) gives access to $ZZ$ correlators but misses the $X$ and $Y$ components of the Pauli correlation structure.
Measuring in three global bases ($Z$, $X$, $Y$) recovers $ZZ$, $XX$, and $YY$ correlators but requires $3\times$ the shot budget and still misses mixed-basis correlators ($XY$, $XZ$, $YZ$).
Classical shadows~\cite{Huang2020} resolve this: by applying a random single-qubit Clifford rotation to each qubit before measurement, all six two-qubit Pauli correlators can be estimated from a single unified measurement protocol, with sample complexity $O(\nqubits^2)$---matching the shot budget required for any individual correlator type.

\paragraph*{Contributions}
\begin{itemize}
    \item A systematic comparison of three polynomial measurement strategies ($Z$-only, multi-basis, classical shadows) for classifying quantum circuit families across 4--20 qubits.
    \item A classical shadows pipeline for quantum circuit classification that extracts all six two-qubit Pauli correlator types ($ZZ$, $XX$, $YY$, $XY$, $XZ$, $YZ$) across all pairs at $O(\nqubits^2)$ shot cost, yielding $3\nqubits^2$ features.
    \item An analysis of the shot requirements of candidate features, motivating the exclusion of Shannon entropy and collision probability and the adoption of a quadratic shot scaling law $\shots = 16\nqubits^2$.
    \item A cross-domain generalization experiment demonstrating that classifiers trained on circuit-sampled data transfer to aggregated spectrum observations, validating that the polynomial feature set captures distribution-level structure beyond the specific measurement context.
\end{itemize}

\section{Background}
\label{sec:background}

\subsection{Circuit Families as Distribution Classes}

We consider three quantum circuit families that span a range of computational complexity.

\paragraph{Clifford circuits}
Generated by $\{H, S, \mathrm{CNOT}\}$, Clifford circuits are efficiently simulable classically via the stabilizer formalism~\cite{Gottesman1998}.
Their output distributions possess structured pairwise correlations reflecting the underlying stabilizer group geometry.

\paragraph{Clifford$+T$ circuits}
Adding $T = \mathrm{diag}(1, e^{i\pi/4})$ yields a universal gate set~\cite{Boykin2000}.
Sufficient $T$-gate density breaks the stabilizer structure and moves the circuit outside the efficiently simulable regime, producing a distributional signature intermediate between Clifford and IQP.

\paragraph{IQP circuits}
Instantaneous Quantum Polynomial circuits apply diagonal gates in the computational basis, sandwiched by Hadamard layers~\cite{Shepherd2009}.
Classical hardness of IQP sampling is supported by complexity-theoretic arguments~\cite{Bremner2016}, and their parity structure produces a distributional fingerprint qualitatively distinct from the Clifford family.

\subsection{Classical Shadows}

Classical shadows~\cite{Huang2020} provide an efficient protocol for estimating many properties of a quantum state from a single randomized measurement dataset.
For each measurement shot, a random single-qubit unitary $U_i$ is applied to each qubit $i$ before measurement in the computational basis, and the rotation choice is recorded alongside the outcome.
The measurement channel can then be inverted classically to produce unbiased estimators for arbitrary observables.

For a single-qubit Pauli $P_i \in \{X, Y, Z\}$, the shadow estimator is:
\begin{equation}
\hat{P}_i = 3 \cdot (-1)^{b_i} \cdot \mathbf{1}[U_i \text{ matches } P_i\text{'s basis}],
\end{equation}
where the factor of 3 corrects for the $1/3$ probability of any given basis being selected.
For a two-qubit correlator $P_i P_j$, the estimator is the product of single-qubit estimators, yielding a factor of $9$ and requiring both $U_i$ and $U_j$ to match their respective Pauli bases.

The connected correlator $\langle P_i P_j \rangle_c = \langle P_i P_j \rangle - \langle P_i \rangle \langle P_j \rangle$ can be estimated by combining single- and two-qubit shadow estimators.
For $K$ Pauli operators, the required number of shadows scales as $O(K / \varepsilon^2)$ for bounded observables, independent of system size~\cite{Huang2020}.
For all $6 \cdot \binom{\nqubits}{2}$ two-qubit Pauli correlators, this yields a total shot cost of $O(\nqubits^2 / \varepsilon^2)$---polynomial in $\nqubits$.

\subsection{Distribution Classification}

\todo{Brief review of relevant prior work on distribution testing, quantum state discrimination, and classical analogs. Position our contribution relative to these.}

\section{Methods}
\label{sec:methods}

\subsection{Circuit Generation}

Circuits are generated using Qiskit~\cite{Qiskit} and simulated with the Aer statevector simulator on GPU.
Each circuit family is parameterized by qubit count $\nqubits \in \{4, \ldots, 20\}$, operations per circuit $N_{\mathrm{ops}} = 1000$, and a reproducible master seed.
For each $(\nqubits, \text{family})$ combination, $N_c = 1000$ unique circuits are generated.
All three measurement strategies are applied to the same set of circuits, enabling a controlled comparison.
Table~\ref{tab:sweep} summarizes the experimental sweep.

\begin{table}[htbp]
\centering
\caption{Experimental parameter sweep.}
\label{tab:sweep}
\begin{tabular}{ll}
\toprule
\textbf{Parameter} & \textbf{Values} \\
\midrule
Circuit families & IQP, Clifford, Clifford$+T$ \\
Qubit counts & 4--20 \\
Shot budget & $16\nqubits^2$ (quadratic) \\
Circuits per family per qubit count & 1000 \\
Operations per circuit & 1000 \\
Simulator & Qiskit Aer (statevector, GPU) \\
Measurement strategies & $Z$-only, multi-basis, shadows \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Shot Budget Design}
\label{sec:shots}

The estimation cost of a statistical feature determines the minimum shot budget needed to estimate it reliably.

\paragraph{Polynomial-cost features}
The Hamming-weight histogram bins all $2^\nqubits$ outcomes into $\nqubits + 1$ weight classes, requiring $O(\nqubits / \varepsilon^2)$ shots.
Single-qubit marginals $P(x_i = 1)$ and parity bias require $O(\nqubits / \varepsilon^2)$ and $O(1/\varepsilon^2)$ shots respectively.
Connected $ZZ$ correlations across all $\binom{\nqubits}{2}$ pairs require $O(\nqubits^2 / \varepsilon^2)$ shots in total, as do all other two-qubit Pauli correlators.
By the classical shadows result~\cite{Huang2020}, \emph{all} $6\binom{\nqubits}{2}$ two-qubit Pauli correlators can be estimated simultaneously at the same $O(\nqubits^2 / \varepsilon^2)$ cost using the shadow protocol.

\paragraph{Exponential-cost features}
Shannon entropy $H = -\sum_x \hat{p}(x)\log_2 \hat{p}(x)$ requires $O(2^\nqubits / \nqubits)$ shots because the empirical frequency estimator is biased whenever bitstrings go unobserved, which becomes the norm above $\sim$10 qubits.
Collision probability $\mathcal{C} = \sum_x p(x)^2$ requires $O(2^{\nqubits/2})$ shots via the birthday problem threshold.
Both are excluded from our feature set.

\paragraph{Quadratic shot scaling}
The most expensive retained features are the two-qubit Pauli correlators, requiring $O(\nqubits^2)$ shots.
We adopt $\shots = 16\nqubits^2$, giving 256 shots at $\nqubits = 4$ and 6400 shots at $\nqubits = 20$.
For the multi-basis strategy, each of the three basis runs receives $\shots / 3$ shots; the total shot budget remains $\shots = 16\nqubits^2$.

\subsection{Measurement Strategies}
\label{sec:strategies}

We compare three measurement protocols, each operating within the same $\shots = 16\nqubits^2$ total shot budget.

\paragraph{$Z$-only baseline}
All shots are taken in the computational basis.
Features extracted: Hamming-weight histogram, $Z$-basis single-qubit marginals, parity bias, and all-pairs connected $ZZ$ correlations.
This is the minimal polynomial-resource strategy and serves as the baseline.

\paragraph{Multi-basis}
Shots are divided equally among three global basis rotations: identity ($Z$ basis), Hadamard on all qubits ($X$ basis), and $S^\dagger H$ on all qubits ($Y$ basis), with $\shots/3$ shots per basis.
Features extracted: Hamming-weight histogram, $Z$/$X$/$Y$ single-qubit marginals, parity bias, and all-pairs connected $ZZ$, $XX$, and $YY$ correlations.
This gives three of the six Pauli pair correlator types at the same total shot cost, at the expense of $3\times$ fewer shots per basis run.
Mixed-basis correlators ($XY$, $XZ$, $YZ$) are not accessible with global rotations.

\paragraph{Classical shadows}
For each of the $\shots$ measurement shots, a random basis ($Z$, $X$, or $Y$) is independently selected for each qubit, and the circuit is measured after applying the corresponding single-qubit rotation.
All six two-qubit Pauli correlator types ($ZZ$, $XX$, $YY$, $XY$, $XZ$, $YZ$) are estimated from the resulting shadow dataset using the unbiased shadow estimators described in Section~\ref{sec:background}.
This achieves the full Pauli correlation structure at the same total shot budget as the $Z$-only baseline.

\subsection{Feature Extraction}
\label{sec:features}

\paragraph{$Z$-only features}
The feature vector concatenates: Hamming-weight histogram ($\nqubits+1$ values), $Z$-marginals ($\nqubits$ values), parity bias (1 value), and all-pairs $ZZ$ connected correlations ($\binom{\nqubits}{2}$ values):
\begin{equation}
d_{\text{Z}} = (\nqubits+1) + \nqubits + 1 + \tfrac{\nqubits(\nqubits-1)}{2} = \tfrac{\nqubits^2 + 3\nqubits + 4}{2}.
\end{equation}
This grows as $O(\nqubits^2)$, dominated by the all-pairs $ZZ$ term.

\paragraph{Multi-basis features}
The feature vector extends the $Z$-only set by appending $X$-marginals and all-pairs $XX$ correlations, then $Y$-marginals and all-pairs $YY$ correlations:
\begin{equation}
d_{\text{MB}} = d_{\text{Z}} + 2\nqubits + \nqubits(\nqubits-1) = O(\nqubits^2).
\end{equation}

\paragraph{Shadow features}
The feature vector concatenates $Z$/$X$/$Y$ single-qubit expectations ($3\nqubits$ values) and all six Pauli pair connected correlations across all $\binom{\nqubits}{2}$ pairs ($6 \cdot \binom{\nqubits}{2}$ values):
\begin{equation}
d_{\text{S}} = 3\nqubits + 6 \cdot \tfrac{\nqubits(\nqubits-1)}{2} = 3\nqubits^2.
\end{equation}
All three feature vectors scale as $O(\nqubits^2)$, consistent with the polynomial-resource design goal.

\subsection{Classification Pipeline}

We evaluate four classifiers from scikit-learn~\cite{scikit-learn}: Logistic Regression (L-BFGS, \texttt{max\_iter}=1000), Decision Tree (unlimited depth), Random Forest (100 estimators), and Support Vector Machine (RBF kernel).
Data are split 80/20 train/test.
Accuracy is reported as the fraction of correctly classified test samples.
Each classifier is independently trained and evaluated for each measurement strategy and qubit count.

\subsection{Cross-Domain Experiment: Circuit Training to Spectrum Observations}
\label{sec:crossdomain_method}

To validate that the polynomial feature set captures distribution-level structure beyond the circuit simulation context, we conduct a cross-domain generalization experiment.
Classifiers are trained exclusively on circuit-sampled data and evaluated on aggregated spectrum observations drawn from the same distribution classes.

Each spectrum observation is formed by drawing $N_s = 50$ circuit feature vectors with replacement and averaging them:
\begin{equation}
\mathbf{x}_{\mathrm{obs}} = \frac{1}{N_s}\sum_{k=1}^{N_s} \mathbf{x}_{(k)}.
\end{equation}
This simulates an observer collecting $N_s$ independent samples from an unknown process and summarizing them as an average feature vector---a natural aggregation strategy when the underlying distribution class, rather than individual outcomes, is the quantity of interest.

We run three experiments:
\begin{enumerate}
    \item \textbf{Circuit-only}: train and test on raw circuit features (80/20 split).
    \item \textbf{Spectrum-only}: train and test on aggregated spectrum observations (80/20 split).
    \item \textbf{Cross}: train on circuit features, test on spectrum observations.
\end{enumerate}
The cross experiment is the primary result: it tests whether the polynomial features generalize across the distribution shift induced by aggregation, and whether the class-separating structure is preserved under the change of data modality.

\section{Results}
\label{sec:results}

\subsection{Measurement Strategy Comparison}

\todo{Three line plots (one per strategy) of accuracy vs.\ qubit count, with one curve per classifier. Does shadows outperform $Z$-only at high qubit counts? Does multi-basis benefit from the additional correlator types, or does the $3\times$ shot dilution hurt?}

\subsection{Classification Accuracy vs.\ System Size}

Figure~\ref{fig:qubit_accuracy} shows average accuracy as a function of qubit count for each measurement strategy.

% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=\linewidth]{qubit_accuracy_z_only.png}
%     \caption{Average classifier accuracy vs.\ qubit count under the $Z$-only measurement strategy, 4--20 qubits.}
%     \label{fig:qubit_accuracy}
% \end{figure}

\todo{Describe scaling trend. Does accuracy degrade, plateau, or improve with qubit count? Is there a crossover where shadows outperforms multi-basis and $Z$-only?}

\subsection{Per-Family Confusion Analysis}

\todo{Include confusion matrix or per-class precision/recall. Which pairs of families are hardest to distinguish? Does shadows improve separation of the hardest pair?}

\subsection{Cross-Domain Generalization: Circuit Training to Spectrum Observations}
\label{sec:cross}

Table~\ref{tab:cross} and Figure~\ref{fig:crossdomain} summarize the three cross-domain experiments.
\todo{Fill in numerical results once data is available.}

% \begin{figure}[htbp]
%     \centering
%     \includegraphics[width=\linewidth]{crossdomain.png}
%     \caption{Cross-domain experiment results: circuit-only, spectrum-only, and cross accuracy by classifier and measurement strategy. Dashed line indicates random-chance baseline ($1/3$).}
%     \label{fig:crossdomain}
% \end{figure}

A consistent pattern emerges: tree-based methods dominate on within-modality tasks but fail to generalize in the cross setting, while Logistic Regression remains reliably above the random-chance baseline when trained on circuits and tested on spectrum observations.

\paragraph{Interpretation}
This result follows directly from the aggregation operation.
By the central limit theorem, averaging $N_s = 50$ feature vectors preserves class means but compresses feature variance by $1/\sqrt{N_s}$.
Tree-based classifiers learn axis-aligned threshold rules calibrated to the training variance; these thresholds become misaligned when variance collapses under aggregation.
Logistic Regression learns a separating hyperplane $\mathbf{w}^\top\mathbf{x} > b$.
Since class means are preserved under averaging ($\mathbb{E}[\bar{\mathbf{x}}] = \mathbb{E}[\mathbf{x}]$), the classes remain on the correct sides of the learned hyperplane, and generalization follows.

Critically, this result validates the polynomial feature set: successful cross-domain generalization is evidence that the Pauli correlators and marginals capture distributional properties of the circuit families that are modality-independent, not artifacts of the specific simulation context.
The classical shadows strategy is of particular interest here: by accessing the full Pauli correlation structure, it provides the richest linear separating signal, and we expect it to show the most robust cross-domain generalization.

\paragraph{Scaling behavior at high qubit counts}
\todo{Describe how cross-domain accuracy behaves for 11--20 qubits once HPC data is available. Does shadows maintain generalization where $Z$-only degrades?}

\section{Discussion}
\label{sec:discussion}

\subsection{Classical Shadows as the Right Measurement Protocol}

The central finding of this work is that classical shadows enables polynomial-resource classification of exponentially large quantum distributions.
The key insight is that the information distinguishing IQP, Clifford, and Clifford$+T$ families is encoded in low-order Pauli correlations---quantities that classical shadows can estimate efficiently.
The full probability distribution, which would require exponential resources to characterize, is not needed.

The comparison between multi-basis and shadows is instructive.
Multi-basis measurements access three of the six Pauli correlator types, but at the cost of $3\times$ shot dilution per basis run.
Classical shadows access all six types at no additional shot cost, by using random per-qubit basis selection rather than fixed global rotations.
Whether the additional correlator types ($XY$, $XZ$, $YZ$) provide sufficient discriminative power to compensate for the inherently noisier per-qubit basis estimates is an empirical question that our experimental comparison directly addresses.

\subsection{Generalization as Validation}

The cross-domain experiment serves a dual purpose.
It is both a practical demonstration---showing that circuit-trained classifiers can be applied to spectrum observations without labeled field data---and a theoretical validation: successful generalization is evidence that the polynomial features identify distribution-level structure rather than simulation artifacts.

This establishes a criterion for the applicability of the framework to new domains: the distribution classes must be linearly separable in the Pauli correlation feature space.
The cross-domain experiment directly tests this condition.

\subsection{Limitations}

This study uses noiseless classical simulation.
Real device noise will alter the distributions and likely degrade classification accuracy.
Extending to noisy simulation (Aer noise models) or real hardware is a natural next step.

The shot budget $\shots = 16\nqubits^2$ is chosen to maintain consistent statistical quality across qubit counts.
The constant factor of 16 is heuristic; a principled choice requires knowing the signal-to-noise ratio of the class-separating Pauli correlators, which is circuit-family-dependent.

\subsection{Path to Broader Application}

\todo{Discuss what it would take to apply this pipeline to a concrete real-world domain: identify what quantity maps to system dimension and sample count, establish which distribution class the process belongs to, generate labeled circuit data, deploy the linear classifier.}

\section{Conclusion}
\label{sec:conclusion}

We have demonstrated that classical shadows provides a polynomial-resource measurement protocol sufficient to distinguish IQP, Clifford, and Clifford$+T$ circuit families across 4--20 qubits.
By accessing all six two-qubit Pauli correlator types ($ZZ$, $XX$, $YY$, $XY$, $XZ$, $YZ$) across all qubit pairs at $O(\nqubits^2)$ shot cost, classical shadows yields an $O(\nqubits^2)$-dimensional feature vector that captures the distributional signatures of these families without requiring exponential measurement resources.

A systematic comparison with a $Z$-only baseline and a multi-basis strategy isolates the contribution of the full Pauli correlation structure and the effect of shot budget allocation.
The quadratic shot budget $\shots = 16\nqubits^2$ is sufficient for all three strategies, confirming that the polynomial-resource design is consistent across the 4--20 qubit range.

The cross-domain generalization experiment---training on circuit data and testing on aggregated spectrum observations---demonstrates that Logistic Regression is the only classifier that generalizes across this distribution shift.
This result provides both a practical tool for cross-modality classification and a validation that the polynomial feature set captures modality-independent distributional structure.
Successful generalization under classical shadows is evidence that the full Pauli correlation structure is the right feature space for this problem.

\todo{State best accuracy achieved and at what resource level.}

\section*{Acknowledgments}
\todo{Funding sources, HPC allocation, institutional acknowledgments.}

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
